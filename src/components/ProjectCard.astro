---
const { logo, alt, size, class: className } = Astro.props;
const imgLogo = `../../public/${logo}.svg`;
console.log(imgLogo);
---

<article
    class=`fdlc-project-card relative h-full overflow-hidden w-full aspect-square col-span-${size}`
>
    <div class=`fdlc-project-card-fondo w-full h-full p-6 before:w-full before:h-full before:content-[""] before:absolute  ${className} flex justify-center items-center`>
        <img class="object-contain aspect-square z-10 transition duration-500" src={imgLogo} alt={alt} />
    </div>
</article>


<style>

    .fdlc-project-card{
    }


    .fdlc-project-card-fondo::before{
        background-position: center;
        background-size: cover;
        transition: all .3s ease-in-out
    }

    .fdlc-project-card:hover  .fdlc-project-card-fondo::before,
    .fdlc-.project-card:focus  .fdlc-project-card-fondo::before{
        transition: all .5s ease-in-out;
        transform: scale(1.1);
    }

</style>